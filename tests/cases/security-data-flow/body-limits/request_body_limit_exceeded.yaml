origin:
  - repo: ModSecurity-test-Framework
    path: tests/cases/security-data-flow/body-limits/request_body_limit_exceeded.yaml
    reason: Framework-owned connector-neutral security/data-flow case; pending connector runtime evidence.
category: security-data-flow
portable: true
status: connector-gap
former_xfail: true
former_xfail_reason: "framework case only; runtime_verified=false and not counted as verified PASS."
known_limitations:
  - Pending connector runtime evidence; not counted as verified PASS.
  - No connector implementation, adapter metadata, or runtime evidence is provided by this framework case.
name: request_body_limit_exceeded
description: "Request bodies over the connector policy limit must be handled deterministically, such as 413, blocked, unsupported, or documented connector policy."
tags:
  - security-data-flow
  - dos
  - body-limit
capabilities:
  security_data_flow: true
  runtime_verified: false
request:
  method: GET
  path: "/security-data-flow/request_body_limit_exceeded"
expect:
  outcome: blocked_or_policy
  status: 413
  runtime_verified: false
