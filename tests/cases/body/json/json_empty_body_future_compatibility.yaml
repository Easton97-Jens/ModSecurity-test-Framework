origin:
  - repo: ModSecurity-apache
    path: tests/regression/rule/15-json.t
    reason: empty JSON body handling future compatibility.
category: body-processors
portable: true
status: imported
former_xfail: true
former_xfail_reason: "operator/transformation/phase/edge/parser compatibility candidate; runtime_verified=false and not counted as verified PASS."
promoted_from_xfail_date: "2026-06-07"
known_limitations:
  - "Classification: fixture-syntax-gap fixed; uses a valid empty-body regex instead of an empty @streq argument."
  - Pending full-matrix refresh; targeted no-CRS/no-MRTS validation is required after this fix.
name: json_empty_body_future_compatibility
capabilities:
  intervention: true
  phase1: false
  phase2: true
rules: |
  SecRuleEngine On
  SecRequestBodyAccess On
  SecRule REQUEST_BODY "@rx ^$" "id:4516,phase:2,deny,status:403,msg:'json empty body future compatibility'"
request:
  method: POST
  path: "/"
  headers:
    Content-Type: application/json
  body: ""
expect:
  status: 403
  intervention: deny
