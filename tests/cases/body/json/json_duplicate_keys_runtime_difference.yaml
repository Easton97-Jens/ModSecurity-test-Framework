origin:
  - repo: ModSecurity-apache
    path: tests/regression/rule/15-json.t
    reason: duplicate JSON keys runtime-difference probe.
category: body-processors
portable: true
status: imported
former_xfail: true
former_xfail_reason: "multipart/files/unicode/json/xml/xss-like/sqli-like compatibility candidate; runtime_verified=false and not counted as verified PASS."
promoted_from_xfail_date: "2026-06-07"
known_limitations:
  - "Classification: runtime-difference."
  - Runtime verification pending; not counted as verified PASS.
name: json_duplicate_keys_runtime_difference
capabilities:
  multipart: false
  files: false
  request_body: true
  transformations: false
rules: |
  SecRuleEngine On
  SecRule REQUEST_BODY "@contains dupkey" "id:4710,phase:2,deny,status:403,msg:'duplicate json keys runtime diff'"
request:
  method: POST
  path: "/"
  headers:
    Content-Type: application/json
  body: |-
    {"dupkey":1,"dupkey":2}
expect:
  status: 403
  intervention: deny
