origin:
  - repo: ModSecurity-apache
    path: tests/regression/rule/10-xml.t
    reason: XML namespace edge connector-gap probe.
category: body-processors
portable: true
status: imported
former_xfail: true
former_xfail_reason: "multipart/files/unicode/json/xml/xss-like/sqli-like compatibility candidate; runtime_verified=false and not counted as verified PASS."
promoted_from_xfail_date: "2026-06-07"
known_limitations:
  - "Classification: connector-gap."
  - Runtime verification pending; not counted as verified PASS.
name: xml_namespace_edge_connector_gap
capabilities:
  multipart: false
  files: false
  request_body: true
  transformations: false
rules: |
  SecRuleEngine On
  SecRequestBodyAccess On
  SecRule REQUEST_HEADERS:Content-Type "@rx ^(?:application|text)/xml$" \
    "id:47110,phase:1,t:none,t:lowercase,nolog,pass,ctl:requestBodyProcessor=XML"
  SecRule XML:/* "@contains 1" "id:4711,phase:2,deny,status:403,msg:'xml namespace edge gap'"
request:
  method: POST
  path: "/"
  headers:
    Content-Type: application/xml
  body: |-
    <ns:root xmlns:ns="urn:test"><ns:a>1</ns:a></ns:root>
expect:
  status: 403
  intervention: deny
