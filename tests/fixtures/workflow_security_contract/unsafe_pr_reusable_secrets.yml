name: Pull request reusable workflow secrets

on:
  pull_request:

permissions:
  contents: read

jobs:
  call:
    uses: actions/checkout/.github/workflows/reusable.yml@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    secrets: inherit
