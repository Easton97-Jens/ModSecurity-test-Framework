name: unsafe-inline-zizmor-fixture

on: [pull_request_target]

permissions:
  contents: write

jobs:
  unsafe-message:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.pull_request.body }}"
