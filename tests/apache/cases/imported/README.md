# Imported Apache-Specific Cases

Status: mapped

Apache-specific cases belong here only when they can be expressed without
copying upstream test files and without requiring hidden Apache::Test harness
state. In this import pass, the reviewed Apache-specific candidates require
Apache context/config inheritance or other httpd harness mechanics, so they are
documented in `../apache-regression-map.md` instead of being ported here.
